# Images and Media Rules

- `main.*` is required and fixed to position 1.
- Secondary images use `02.*`, `03.*`, ... with no gaps.
- Media identity is filename marker in alt text (`sync:<filename>`).
- File hashes (SHA256) determine incremental upload decisions.
- Never map images by title.
