# Repo Structure Rules

- `theme/` contains Shopify theme source.
- `products/` contains product folders named by handle.
- `catalog/` stores generated manifests and local sync state.
- `scripts/` is the only location for automation logic.
- `docs/` stores user/operator documentation.
- `skills/` stores policy and operational playbooks.
