# Theme Development

- Keep theme source under `theme/`.
- Use `bash scripts/theme_push.sh` for CLI-based theme deployment.
- Shopify GitHub integration can be used as an alternative deployment path.
